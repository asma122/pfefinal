import { Authentificationresponse } from './authentificationresponse';

describe('Authentificationresponse', () => {
  it('should create an instance', () => {
    expect(new Authentificationresponse()).toBeTruthy();
  });
});
